# Drops beta relative to its sibling v1. A cluster moving v1 -> v2 has beta pruned.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: alpha
    literals: [k=v]
